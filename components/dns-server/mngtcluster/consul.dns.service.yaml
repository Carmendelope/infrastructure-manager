apiVersion: v1
kind: Service
metadata:
  labels:
    app: consul
    release: dns-test
  name: dns-test-consul-dns
  namespace: nalej
spec:
  ports:
  - name: dns-tcp
    port: 53
    protocol: TCP
    targetPort: dns-tcp
  - name: dns-udp
    port: 53
    protocol: UDP
    targetPort: dns-udp
  selector:
    app: consul
    hasDNS: "true"
    release: dns-test
  sessionAffinity: None
  type: ClusterIP
